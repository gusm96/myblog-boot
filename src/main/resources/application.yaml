##Database
spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3307/myblog
    username: root
    password: moya134353@@
  jpa:
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    open-in-view: false
  sql:
    init:
      mode: always
      platform: mariadb
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 6000

## Logging
logging:
  level:
    org:
      hibernate:
        sql: debug
        type: trace

## JWT
jwt:
  secret: moya.myblog.secret.key
  access-token-expiration: 300000
  refresh-token-expiration: 1209600000

## Image File Path
file:
  upload-dir: C:/Users/gusm9/Documents/workspace/myblog-boot/images/

## 사용할 관리자 정보를 입력하세요.
admin:
  username : "moyada123"
  password : "moyada123"
  nickname : "Moya"