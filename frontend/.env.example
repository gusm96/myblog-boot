# 백엔드 API 주소 (서버 컴포넌트 fetch, 클라이언트 fetch 모두 사용)
# 로컬 개발
NEXT_PUBLIC_API_URL=http://localhost:8080

# Docker Compose 운영 환경 (docker-compose.yaml의 NEXT_PUBLIC_API_URL ARG로 빌드 시 주입)
# NEXT_PUBLIC_API_URL=http://backend:8080
