package com.moya.myblogboot;

/**
 * 통합 테스트 기반 클래스.
 * Redis는 application-test.yaml에 설정된 localhost:6379 (DB 1)를 사용한다.
 * Docker/Testcontainers 없이 로컬 Redis 인스턴스에 의존한다.
 */
public abstract class AbstractContainerBaseTest {
}
